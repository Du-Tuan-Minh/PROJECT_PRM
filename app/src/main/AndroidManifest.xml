<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Internet and Location -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_PRM"
        tools:targetApi="31">

        <!-- Login Activity (Khởi động ứng dụng) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- Main Activity (Dashboard) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Disease Library Activity -->
        <activity
            android:name=".MainScreen.DiseaseLibraryActivity"
            android:exported="false" />

        <!-- Find Clinic Activity -->
        <activity
            android:name=".MainScreen.FindClinicActivity"
            android:exported="false">

        </activity>

        <!-- Book Appointment Activity -->
        <activity
            android:name=".MainScreen.BookAppointmentActivity"
            android:exported="false" />

        <!-- Chatbot Activity -->
        <activity
            android:name=".MainScreen.ChatbotActivity"
            android:exported="false" />

    </application>

</manifest>